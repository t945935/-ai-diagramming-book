# 第 11 章驗證紀錄

日期：2026-09-11。Python 3.12.3，Linux／WSL。

## 已執行

- 讀取六套工具的相關來源；概念對照保存八份來源 SHA-256（含 Graphviz 資料與生成程式）。
- 四個概念下十個引用比對成功。XML／JSON 項目按 ID 與標籤比對；D2／Mermaid／PlantUML 用所列來源片段比對。這不是通用 AST 驗證或語意等價證明。
- D2／draw.io 的四條呼叫端點各自與明示期望集合比對，一個測試方法通過。
- 只在暫存 D2 副本移除資料庫邊，同一測試方法確實失敗，訊息為 `D2 edge count differs`；原始來源測試隨後通過。完整輸出見 contract-test-output.json。
- 交付前再次比對八份原圖／生成器來源的 SHA-256，沒有改動既有圖。

全套 Python 測試另外重跑，共八個測試方法通過，無略過；[完整輸出](suite-output.txt)。其中既有除錯測試會在暫存目錄啟動 Mermaid 與 dot，此次沒有重產既有六套範例預覽。

## 限制

- 不驗證兩張圖的標籤相同；已知「開啟頁面」與「HTTPS」並不相同。
- 契約測試不是 D2 通用 parser，只支援本例單行連線。也不驗證 draw.io 樣式是否顯示箭頭、是否被遮住等視覺問題。
- 沒有實際做跨格式轉換、服務改名、六套 GUI 編輯或既有六套範例的完整重新渲染。
- 映射是明示的教學範圍判定，不能自動推定模組部署到哪一個服務。
- 變更演練是未批准計畫，不是真實已執行變更。
- 尚未跨平台或 CI 驗證。
