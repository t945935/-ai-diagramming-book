# 部分退款綜合案例驗證

日期：2026-09-11。Linux／WSL，Python 3.12.3、Node.js v24.21.0、Mermaid CLI 11.17.0、PlantUML 1.2026.8、既有 Temurin JRE 21。沿用本機 Chromium 與中文字型，未做乾淨安裝。

## 已執行

- 六個額度情境及四個結果快照由兩個規格測試方法檢查通過。
- 暫存副本將未知結果占用從 300 改為 0，測試確實失敗；正常來源重新通過。輸出見 test-evidence.json。
- 更新 Makefile 後，本機 check-core 六個方法、check-full 十個方法均通過，無略過；見 suite-output.txt。
- Mermaid 受理圖 SVG／PNG、PlantUML 狀態圖語法檢查與 SVG／PNG 均 exit 0，見 render-results.json。
- 兩份 SVG 可解析，兩份 PNG 檔頭有效，最終 PNG 經視覺檢查。
- 先前跨工具對照保存的八份舊來源指紋未變；本章沒有重寫既有付款圖。

## 視覺修正

Mermaid 初版包含受理及全部結果分支，出現較長交叉線及小字。拆成受理圖加狀態圖後消除主要交叉，再縮短底部交接文字以避免「失敗」拆字；每次均重新渲染。最終中文完整、無明顯裁切或重疊，縮圖字仍偏小、留白不均。

PlantUML 四狀態與規則說明完整；少數線文間距偏小，但仍可讀。尚未做紙本尺寸驗收。

## 不代表已完成的項目

測試只驗證教學快照及額度公式，沒有退款 API、支付服務、資料庫交易、真正併發、回呼認證、唯一約束、可靠送出或崩潰恢復測試。同鍵不同內容的拒絕目前是圖文規格，沒有實作 API 測試。

本章沒有重畫另外四套圖，理由見 change-impact.md；也沒有多人協作、跨平台、正式 Release 或授權變更。CI 沿用第 12 章 workflow 執行更新後的 check-core；本檔記錄本機結果，遠端結果應查看對應提交 SHA 的 Actions，而非假設推送就一定成功。
